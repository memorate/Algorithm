## db.setting文件

url = jdbc:mysql://3306-W-T-MC-PRD-03-MYC10.service.dcnh.consul/mxbase
user = meixin
pass = mIx2Rn8op

  ## 可选配置
  # 是否在日志中显示执行的SQL
showSql = true
  # 是否格式化显示的SQL
formatSql = false
  # 是否显示SQL参数
showParams = true
  # 打印SQL的日志等级，默认debug，可以是info、warn、error
sqlLevel = debug